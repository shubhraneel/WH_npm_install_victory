// import { selectSocietyDetailsPageDomain } from '../selectors';

describe('selectSocietyDetailsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
