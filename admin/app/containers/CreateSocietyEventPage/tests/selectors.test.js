// import { selectCreateSocietyEventPageDomain } from '../selectors';

describe('selectCreateSocietyEventPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
