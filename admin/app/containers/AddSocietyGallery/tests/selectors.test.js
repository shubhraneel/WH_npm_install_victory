// import { selectAddSocietyGalleryDomain } from '../selectors';

describe('selectAddSocietyGalleryDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
