// import { selectSocietyOfficialPageDomain } from '../selectors';

describe('selectSocietyOfficialPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
