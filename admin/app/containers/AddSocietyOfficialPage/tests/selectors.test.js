// import { selectAddSocietyOfficialPageDomain } from '../selectors';

describe('selectAddSocietyOfficialPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
