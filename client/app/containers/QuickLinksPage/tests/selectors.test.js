// import { selectQuickLinksPageDomain } from '../selectors';

describe('selectQuickLinksPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
