// import { selectAchievementsPageDomain } from '../selectors';

describe('selectAchievementsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
