// import { selectTsgContactsPageDomain } from '../selectors';

describe('selectTsgContactsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
