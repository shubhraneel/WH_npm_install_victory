// import { selectEventsCategoryPageDomain } from '../selectors';

describe('selectEventsCategoryPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
