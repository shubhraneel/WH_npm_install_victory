// import { selectNotificationsPageDomain } from '../selectors';

describe('selectNotificationsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
