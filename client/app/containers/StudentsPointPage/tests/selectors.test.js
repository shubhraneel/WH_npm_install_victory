// import { selectStudentsPointPageDomain } from '../selectors';

describe('selectStudentsPointPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
