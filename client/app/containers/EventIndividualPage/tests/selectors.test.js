// import { selectEventIndividualPageDomain } from '../selectors';

describe('selectEventIndividualPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
